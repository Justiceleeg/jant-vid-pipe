import type { Asset, AssetUploadResponse, AssetStatus } from './asset.types';

// Brand assets use the generic asset types
export type BrandAsset = Asset;
export type BrandAssetUploadResponse = AssetUploadResponse;
export type BrandAssetStatus = AssetStatus;

