import type { Asset, AssetUploadResponse, AssetStatus } from './asset.types';

// Character assets use the generic asset types
export type CharacterAsset = Asset;
export type CharacterAssetUploadResponse = AssetUploadResponse;
export type CharacterAssetStatus = AssetStatus;

