# Environment Configuration
ENVIRONMENT=development

# API Keys
# Get your Replicate API token from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_api_token_here
# Alternative: Some users might use REPLICATE_API_KEY instead
# REPLICATE_API_KEY=your_replicate_api_key_here

# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration (Optional - uses defaults if not set)
# REPLICATE_IMAGE_MODEL=stability-ai/sdxl:39ed52f2a78e934b3ba6e2a89f5b1c712de7dfea535525255b1aa35c5565e08b
# OPENAI_MODEL=gpt-4o

# Image Generation Configuration (Optional - uses auto defaults if not set)
# IMAGES_PER_MOOD=0  # 0 = auto (2 for dev, 4 for prod)
# IMAGE_WIDTH=0      # 0 = auto (640 for dev, 1080 for prod)
# IMAGE_HEIGHT=0     # 0 = auto (1136 for dev, 1920 for prod)

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000
FIREBASE_STORAGE_BUCKET=jant-vid-pipe-fire.firebasestorage.app